#ifndef P_SGAPRED_H
#define P_SGAPRED_H

#include <QDialog>

namespace Ui {
    class p_sgapredForm;
}

class p_sgapredForm : public QDialog {
    Q_OBJECT
public:
    p_sgapredForm(QWidget *parent = 0);
    ~p_sgapredForm();

protected:
    void changeEvent(QEvent *e);

private:
    Ui::p_sgapredForm *ui;

private slots:
    void CloseWindow();
    void SaveRecord();

};

#endif // P_SGAPRED_H
